apiVersion: v1
kind: Service
metadata:
  name: roman-arabic-converter
  labels:
    app: roman-arabic-converter
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: roman-arabic-converter
